~version: "2.1"

# =====================================================
# Telegram Instant View Template for iptvprovider.me
# Submit at: https://instantview.telegram.org/
# =====================================================

# Match all blog article pages
?path: /blog/.+

# =====================================================
# MAIN CONTENT SELECTORS (XPath)
# =====================================================

# Title - The main H1 headline
title: //h1

# Body - Main content container
body: //main

# Cover image - Multiple XPath fallbacks (Telegram tries in order)
# 1. OpenGraph image (best quality, defined in metadata)
cover: //meta[@property="og:image"]/@content

# 2. Twitter Card image fallback
cover: //meta[@name="twitter:image"]/@content

# 3. First image in main content
cover: //main//img/@src

# 4. Any image on page as last resort
cover: //img/@src

# Author (static - no author element in DOM)
author: "IPTV Expert Team"

# Site name
site_name: "IPTV Provider"

# =====================================================
# ELEMENTS TO REMOVE (XPath)
# =====================================================

# Navigation elements
@remove: //header
@remove: //footer
@remove: //nav

# All interactive buttons
@remove: //button

# Fixed/sticky elements (WhatsApp widget, etc.)
@remove: //div[contains(@class, "fixed")]
@remove: //a[contains(@href, "wa.me")]

# Remove internal link clusters at bottom
@remove: //section[.//h2[contains(text(), "Comparison Guides")]]
@remove: //section[.//h2[contains(text(), "Technical Help")]]

# Remove author bio section
@remove: //section[.//h3[contains(text(), "Expert Review Team")]]

# Remove sources section
@remove: //section[.//h2[contains(text(), "Sources")]]
